# This file overrides default nginx HTTP settings for 127.0.0.1:8501
# Mount this file as "/var/lib/nginx-conf/127.0.0.1:8501.conf.erb"

server {
    listen       80;
    server_name  <%= domain.name %>;

    location / {
       return    301 https://$server_name$request_uri;
    }

    # This helper sets up the path for the ACME server to verify that you own the site
    # Defaults to /.well-known/acme-challenge/
    <%= acme_challenge_location %>
}